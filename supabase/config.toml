# Supabase CLI Project Configuration
# Linked to hosted project: ampqcpxpshaiiquqbmmn.supabase.co
# DO NOT use `supabase start` — we use the hosted project only.

[project]
id = "ampqcpxpshaiiquqbmmn"

[api]
# The hosted project REST API is available at:
# https://ampqcpxpshaiiquqbmmn.supabase.co/rest/v1/

[auth]
# Auth model: Username + Password (no phone OTP / SMS / email OTP).
# Workers and supervisors authenticate via username and password.
# Mapped internally to <normalized_username>@armor.internal.
site_url = "https://armor-app.example.com"

[storage]
# Storage buckets (configured via dashboard or migrations):
# - certificates (private) — PDF certificate files
# - avatars (public) — worker profile photos

[functions]
# Edge Functions are in supabase/functions/
# Deploy with: supabase functions deploy <name> --project-ref ampqcpxpshaiiquqbmmn
